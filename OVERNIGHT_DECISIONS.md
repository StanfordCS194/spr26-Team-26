# Overnight Decisions

## Active Assumptions

- Use `codex/tinker-integration` as the base for new stacked work unless a task specifically depends on #40.
- Keep #40 as the integration branch for DataGen reconciliation, not as a dumping ground for unrelated fixes.
- Prefer small live validations before full pipeline runs.
- If a live test fails because a credential is invalid or a provider rejects access, record the failure and pivot to local/fake validation plus a clear PR note.

## Decisions

- 2026-05-19 05:20 PDT — Created `codex/overnight-coordination` worktree for progress logs so feature branches remain clean.
- 2026-05-19 05:30 PDT — Treat raw Mode C web acquisition as source material, not trainable SFT data. Curation must reject targetless web rows until a real structuring stage produces assistant targets.
- 2026-05-19 05:30 PDT — Add explicit Mode C backend control: `auto` can fall back, `synthetic` never calls web, `web` fails loudly. `DATA_GENERATOR_SYNTHETIC_OFFLINE=1` overrides all of them for deterministic local tests.
- 2026-05-19 05:31 PDT — Add `testpaths = tests` / `norecursedirs = outputs/reference` because the local gitignored Tinker cookbook reference checkout should not be collected by repo pytest.
- 2026-05-19 05:32 PDT — Keep the full live HF retrieval suite out of broad smoke commands. It enumerates and downloads many datasets and is a separate external validation, not a cheap overnight regression check.
- 2026-05-19 05:36 PDT — For live Tinker smoke budgeting, use conservative $2.50 ledger entries even when local token/cost reports are much lower. This keeps the $100 cap safe until authoritative billing is exposed.
- 2026-05-19 05:45 PDT — Use `LAST_ASSISTANT_MESSAGE` for Tinker chat/SFT V1 and split multi-assistant conversations into one target per assistant turn. This follows the cookbook-safe path for renderers without the extension property.
- 2026-05-19 05:48 PDT — Web acquisition and web structuring remain separate stages. Raw web pages are source material only; teacher structuring must produce schema-valid SFT rows before curation/training can proceed.
- 2026-05-19 05:49 PDT — Before spending more live budget, run a local stacked merge smoke across all active implementation PRs. The current stack merges cleanly and passes the non-live suite, so the next risk to chase is behavior, not branch compatibility.
- 2026-05-19 05:53 PDT — Treat AutoResearch proposal application as two coordinated artifacts: persist the JSON patch for audit/revert, but pass the pending patch into the SDK-native Tinker run directly from graph state.
- 2026-05-19 05:56 PDT — Keep empty or failed DataGen outputs from entering DecisionEngine/AutoResearch. The Manager boundary should explain untrainable data before the failure reaches Tinker.
- 2026-05-19 06:02 PDT — AutoResearch candidate acceptance should be monotonic against the current best score, not merely better than the original baseline. The diary should report the patch relative to the pre-patch config for auditability.
- 2026-05-19 06:06 PDT — Treat LLM-generated hyperparameter patches as untrusted until validated by `TrainingConfig.apply_patch`; prompt constraints are guidance, runtime validation is the guardrail.
- 2026-05-19 06:13 PDT — Keep the spec-site source of truth aligned with the stacked implementation branches. Since reviewers use the Vercel preview to understand intended behavior, stale REST-job language is treated as an implementation risk, not just documentation drift.
- 2026-05-19 06:22 PDT — Keep the first frontend/backend bridge intentionally thin: start a run, poll state, and surface final Manager results. Live per-stage streaming should wait for run-scoped observability rather than fabricating precision.
- 2026-05-19 06:26 PDT — Add an explicit integration PR for the draft stack rather than assuming sibling branches compose. Retarget docs behind this integrated stack so review order matches implementation reality.
- 2026-05-19 06:29 PDT — Preserve chat `messages` as a first-class data contract through DataGen and curation. Flattening chat records to `input`/`output` loses system and multi-turn context before Tinker ever sees it.
- 2026-05-19 06:53 PDT — Treat `tests/data_generator/test_mode_b_hf_retrieval.py` as live external validation, not part of the normal non-live stack suite. Broad local commands must explicitly ignore it unless the goal is a dedicated Hugging Face retrieval run.
- 2026-05-19 06:54 PDT — Keep #51 as the integration proof branch, but do not push leaf PR commits into their draft base after opening the leaves. GitHub treats that as a merge. Future integration smokes should use local throwaway merge worktrees or unpublished branches until review order is settled.
- 2026-05-19 07:12 PDT — Treat zero loss from a live Tinker run as suspicious unless the SDK explicitly reports a real zero. Missing or unrecognized loss metrics should fail loudly rather than becoming `primary_metric=1.0`.
- 2026-05-19 07:19 PDT — Small live smoke datasets should still reserve a validation holdout when possible. For 3+ curated examples, zero validation rows is a quality bug, not just a cosmetic split count.
- 2026-05-19 07:22 PDT — Do not use an absolute finite loss threshold as an AutoResearch catastrophe signal for Tinker SFT. LLM cross-entropy can start above 10 on tiny smoke data; non-finite loss and primary metric collapse are the safer early-stop triggers.
- 2026-05-19 07:22 PDT — Keep integration smokes on unpublished local branches and verify the expected leaf commits are present before running expensive checks. Detached throwaway checkouts were too easy to misread after earlier resets.
- 2026-05-19 07:22 PDT — Treat #51 as an integration proof, not as a normal feature PR. The review order should land its ancestor branches first, then refresh #51 or replace it with a final composition PR.
- 2026-05-19 07:29 PDT — The first real-proposer live run produced a useful signal on the small web-SFT dataset: `learning_rate=0.0005` looked better than `0.0001` at two steps. Validate that signal with a slightly longer paired run before turning it into a default or recommendation.
- 2026-05-19 07:32 PDT — For the current Tinker V1 artifacts, final `metrics.json` should summarize observed training-step loss rather than mirror the last batch. Tiny smoke datasets cycle unevenly, so last-batch-only scoring adds avoidable noise to AutoResearch comparisons.
- 2026-05-19 07:40 PDT — AutoResearch candidate acceptance needs a material-improvement threshold. On noisy tiny live runs, a +0.1% relative change is effectively a tie and should not update best config or reset the search history.
- 2026-05-19 07:51 PDT — The `learning_rate=0.0005` signal now reproduced on both the 6-row hand-built web sample and the 22-row real DataGen graph dataset. Treat it as the current best smoke setting for this customer-support priority task, but not as a global default.
- 2026-05-19 08:00 PDT — When DataGen provides split counts, Tinker scoring should use the validation/test holdouts instead of the training-step proxy. Path-only datasets keep the old all-train behavior because they do not carry enough split metadata.
- 2026-05-19 08:03 PDT — Continue using conservative ledger estimates for Tinker even when local reported token spend is tiny. The live heldout eval adds only one extra small forward pass over val/test splits, but we still book it as a standard bounded smoke.
- 2026-05-19 08:08 PDT — `dataset_path` remains for compatibility, but AutoResearch needs the aggregate `StandardDataset` on `TrainingPlan` to preserve curation split counts. This is intentionally lighter than carrying the entire `DatasetResult` through the plan.
- 2026-05-19 08:11 PDT — The heldout-scored full graph reproduces the earlier learning-rate signal while using validation split loss instead of training proxy loss. Keep using deterministic one-candidate graph smokes for wiring checks; reserve real-proposer multi-candidate loops for behavior questions.
- 2026-05-19 08:13 PDT — Source split labels should win over synthetic split heuristics when available. This makes HF/Mode B validation boundaries usable by the same heldout Tinker path without changing unlabeled Mode C behavior.
- 2026-05-19 08:19 PDT — Heldout scoring should respect the same batch-size constraint as training. Validation/test forward passes can be larger than training on real datasets if they are not explicitly batched.
- 2026-05-19 08:25 PDT — For SDK-native Tinker SFT, the executable plan metric is `primary_metric`, even when the task analysis says the user-facing task is classification. Task-level metric names should not leak into the proxy scorer.
- 2026-05-19 08:55 PDT — Budget preflight belongs before `cost_manager.start()` and before any Tinker SDK call. However, `estimated_cost` is a plan-level estimate, not a per-launch estimate, so PR #69 uses explicit `estimated_run_cost_usd` for optional stricter gating and otherwise relies on remaining-budget exhaustion checks plus post-run recorded spend.
- 2026-05-19 09:00 PDT — DecisionEngine should populate `estimated_run_cost_usd` from bounded run shape, not force AutoResearch to infer it from the full plan estimate. This keeps cheap smoke/candidate runs preflightable without making every iteration look like full training.
- 2026-05-19 09:05 PDT — For budget enforcement, a launched Tinker run should debit at least its per-launch estimate when the runner reports a lower token-ledger cost. This is intentionally conservative and prevents repeated launches from bypassing the budget guard.
- 2026-05-19 09:14 PDT — Treat Manager LLM output as untrusted even when the prompt says "raw JSON only." The boundary should strip common JSON fences, validate required keys, and raise clear errors rather than leaking raw parser failures out of LangGraph.
- 2026-05-19 09:17 PDT — Do not keep adding live validations until the spend floor has been proven at the Manager boundary. The latest Mode B run stopped exactly at the `$2.24` software budget, so the remaining budget can be used for targeted bug hunts rather than repeated budget mechanics checks.
- 2026-05-19 09:17 PDT — Root/global output paths are now the next integration risk. `DECISIONS_LOG` is import-time global state, and several other paths (`configs/current.json`, `outputs/datasets/train_data.jsonl`, `outputs/logs/research_diary.jsonl`) are still shared across runs. Fixing run-scoped output isolation should be treated as API-readiness work, not a Tinker runner issue.
- 2026-05-19 09:25 PDT — Use a contextvars runtime context for API/background run output isolation instead of process-wide `chdir` or per-call plumbing through every layer. This preserves CLI defaults while making server-thread runs safe to compose.
- 2026-05-19 09:25 PDT — Do not salvage old PR #27 crawler work. The current DataGen stack already has those capabilities; the remaining concern is stack topology (#44 web structuring needs reconciliation with the latest DataGen leaves), not missing code from #27.
